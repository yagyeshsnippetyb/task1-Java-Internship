# task1-Java-Internship This project demonstrates the basic setup and execution of a Java program using OpenJDK and VS Code. 
It includes creating a simple Java class, compiling it into bytecode, and running it from the terminal. 
The program also accepts and displays command-line arguments to show runtime input handling.
